@model IEnumerable<QLNV_VC.Models.PhongBan>

@{
    ViewBag.Title = "Thông tin phòng ban";
    Layout = "~/Views/Shared/_LayoutAdmin.cshtml";
}

<h2>Thông tin phòng ban</h2>

<p>
    @Html.ActionLink("Tạo mới", "Create")
</p>
<table class="table">
    <tr>
        <th>
            Tên phòng ban
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TenPB)
        </td>
        <td>
            @Html.ActionLink("Sửa", "Edit", new { id=item.MaPB }) |
            @Html.ActionLink("Chi tiết", "Details", new { id=item.MaPB }) |
            @Html.ActionLink("Xóa", "Delete", new { id=item.MaPB })
        </td>
    </tr>
}

</table>
